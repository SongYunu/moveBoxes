# Medium v2.2: 집기 높이 보정

[Colab에서 열기](https://colab.research.google.com/github/SongYunu/moveBoxes/blob/main/notebooks/moveboxes_medium_v22_colab.ipynb)

v2.1 테스트 궤적에서는 첫 실패 후 XY 오차가 약 5~10mm까지 줄었지만 TCP가 상자 중심보다
약 63~79mm 높은 상태에서 그리퍼를 닫았습니다. 성공 집기는 약 30~40mm 차이에서 발생합니다.
v2.2는 기존 단계 판정과 관측 인코더를 고정하고 행동 디코더만 보정합니다.

- 집기 단계의 Z축 오차: 기본의 3배
- 그리퍼를 닫는 접촉 구간의 Z축 오차: 기본의 6배
- 하강 중 열기와 접촉 때 닫기: 기본의 3배
- 시간 가중치: v2.1과 같이 네 상자를 모두 성공한 시연끼리만 적용
- 평가: 매 500회, 최대 4,000회, 개선 없는 4구간 뒤 종료

## 실행

Colab 2026.07 / Python 3.12 / T4 GPU에서 01부터 순서대로 실행합니다.

| 셀 | 작업 |
|---|---|
| 01~04 | 설정, 코드, GitHub 연결, 설치 |
| 05 | GPU 확인 후 v2.1 선택 모델과 검증된 32개 시연 복원 |
| 06 | 시연의 실제 집기 높이 검사 |
| 07 | Z축 보정 학습과 매 500회 개발 평가 |
| 08 | 선택 모델 별도 테스트, 영상, 행동 궤적 |
| 09 | 구간별 결과 확인 |
| 10 | 별도 시드 최종 100회 평가 |
| 11 | 제출용 패키지 저장 |

새 시연 수집은 하지 않습니다. 셀 07이 끊기면 새 런타임에서 01~05를 실행하고 07을 다시 실행하면
마지막 500회 체크포인트부터 이어집니다. 결과는 기존 v2.1과 분리된
`run-moveboxes_medium_zfocus_v22_benchmark` Release에 저장됩니다.

개발 평가에서 보정 모델이 원본보다 나쁘면 `lab_best.json`은 원본 모델을 계속 선택합니다.
셀 08 영상에서 그리퍼가 열린 채 약 6cm까지 내려간 뒤 닫는지 먼저 확인하세요.

## 가벼운 백업

셀 07은 각 500회 구간이 끝날 때 최고로 선택된 모델과 점수·설정만 GitHub에 저장합니다.
`latest.pt`, optimizer, 이전 블록 모델, 수집 데이터 복사본과 학습 로그는 현재 Colab 런타임에만 둡니다.
원본 모델이 계속 최고라면 모델 파일도 하나만 저장하고, 보정 모델이 최고가 되면 원본과 최고 모델 두 개만 저장합니다.
새 런타임의 셀 05는 32개 시연을 v2.1 Release에서 다시 내려받습니다. 따라서 복원은 조금 다운로드하지만
매 학습 구간마다 수백 개 파일을 다시 백업하지 않습니다.
