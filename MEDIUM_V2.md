# Medium v2 — 제한 시간 안의 집기·분류 보정

[Colab에서 열기](https://colab.research.google.com/github/SongYunu/moveBoxes/blob/main/notebooks/moveboxes_medium_v2_colab.ipynb)

Colab **2026.07 / Python 3.12 / T4**를 사용합니다. 코드 셀만 12개입니다.
기존 Easy와 Medium v1 노트북·결과를 유지하고, 새 실행 `moveboxes_medium_deadline_v2`로 저장합니다.

| 셀 | 실행 내용 |
|---|---|
| 01~04 | 설정 → 코드 로드 → GitHub 인증/복원 → 설치 |
| 05 | GPU 확인, Medium v1에서 개발 평가로 선택된 최고 모델 가져오기 |
| 06 | 빠른 시연 시험: 이동 gain 2개 × 시드 2개, 총 4회 |
| 07 | 199행동 시점에 네 상자 정답이 확인된 시연 16개 수집 |
| 08 | 보정 전 기준 점수 측정 → 500회 학습·8회 평가 반복 |
| 09 | 최고 모델 별도 8회 테스트·영상·행동 기록 |
| 10 | 구간별 성적 확인 |
| 11 | 선택한 모델로 별도 시드 최종 100회 평가 |
| 12 | 평가를 완료한 모델 패키징 |

수집은 셀 한 번당 약 10분의 예산을 두며, 진행 중인 에피소드 저장 후 멈춥니다.
시간 예산으로 멈춘 경우 같은 셀을 다시 실행합니다. 성공 시연 수가 부족하면 학습을 시작하지 않습니다.
시험에서 제한 내 성공이 하나도 없으면 `medium/collection/manifest.json`에 실패 기록을 남기고 긴 수집·학습을 막습니다.
실제 Colab 물리 환경에서 이 시험이 통과하는지는 아직 검증하지 않았습니다.

## 변경 이유와 동작

Medium v1 기록에서 헛집기 후 재시도에 31행동을 사용했고, 다른 실행에서는 네 번째 집기 시도가 197번째 행동에 있었습니다.
원본 Medium 시연 200개의 길이는 234~238, 복구 시연 16개는 209~271이었습니다.
이 길이는 저장된 전체 궤적 길이이며 최초 성공 시점과 동일하다고 단정하지 않습니다.
v2는 길이 대신 **공식 평가와 같은 199행동 후의 실제 `success_count`**로 수집 시연을 합격 판정합니다.
환경 설정의 `max_episode_steps=200`과 공식 `rollout_metrics`의 `range(max_steps-1)`을 그대로 따릅니다.

새 수집용 교사는 먼 거리 이동을 가속하고 마지막 2cm에서는 원래 비례 제어 이득을 사용합니다.
집기 전 가로 오차 5mm와 높이를 확인하고, 실제 grasp 플래그가 연속으로 확인되어야 들어 올립니다.
실패하면 같은 상자를 재정렬하며, 시간 경과만으로 다음 상자로 넘어가지 않습니다.
이 교사는 **훈련 데이터 생성에만 사용**하며 제출 정책은 기존 크기의 학습된 Stage ACT입니다.

성공 시연의 실제 관측과 교사의 깨끗한 행동을 짝지어 학습합니다.
수집 중 작은 행동 교란으로 발생한 실제 상태도 포함하고, 교란된 행동은 정답으로 쓰지 않습니다.
다음 프레임부터의 학습 목표가 맞지 않게 되는 교란 경계에서는 행동 청크를 자릅니다.
첫 상자뿐 아니라 **모든 상자의 낮은 높이 집기 구간을 35%**, 단계 전환을 25%, 전체 작업을 40% 표집합니다.
에피소드 단위로 학습/검증을 분리하고, 이 비율은 학습 데이터에만 적용합니다.

기존 `medium/lab_best.json`이 선택한 모델을 가져옵니다. 현재 사용자 실행에서는 4,000회 모델이며 마지막 12,000회 모델과 다릅니다.
원본 HDF5 및 느린 복구 시연은 v2 보정 학습에 섞지 않으며, 큰 데이터 ZIP 다운로드도 생략합니다.

## T4 예산과 결과 보존

- 기존 약 1.4M 모델 구조, 배치 32, AMP, 학습률 `3e-5`.
- 최대 500회 × 6구간 = 3,000회. 2구간 개선이 없으면 조기 종료합니다.
- 각 구간 후 평균화 설정 4 하나만 8회 평가해 탐색 비용을 줄입니다.
- 보정 전 모델도 **동일한 새 개발 시드**로 평가합니다. 보정 모델이 더 나쁘면 시작 모델이 최고 모델로 유지됩니다.
- 개발 목표 95%는 조기 종료 목표이며 성능 보장이 아닙니다. v2는 개발 분류 점수가 0보다 크면 부분 점수 모델도 최종 평가·패키징할 수 있습니다.
- 개발 52000, 테스트 42000, 최종 72000, 시연 시험 120000, 시연 수집 121000부터의 시드를 사용합니다.

결과는 `run-moveboxes_medium_deadline_v2_benchmark` Release에 자동 저장합니다.
새 계정/런타임에서는 같은 설정으로 01~05를 실행한 뒤 중단한 수집·학습 셀을 재실행합니다.
완료 에피소드, 체크포인트·optimizer·RNG, 끝난 평가를 재사용합니다. 학습 설정 변경 시 새 `run_name`을 쓰세요.

CPU 테스트는 제한 시점 성공 판정, 집기 확인/재정렬, 시연 분리·검증, 수집 재개, 학습 재개,
기존 최고 모델 보존과 부분 점수 패키징을 검증합니다. 실제 성공률 향상과 전체 실행 시간은 Colab에서 측정해야 합니다.
